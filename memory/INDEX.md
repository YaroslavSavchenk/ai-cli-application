# Memory Index

Map of content for the project memory vault. One line per note, newest-first
within sections. Update this file every time a note is added, renamed, or
superseded. Conventions live in `.claude/skills/memory/SKILL.md`.

## Backlog

- [[BACKLOG]] — **the living to-do list**: CI/CD leftovers (branch protection = user decision, janitor pass, server-side settle, Dependabot, npm audit), Windows-side checks owed, open decisions (repo visibility, one-click installer), queued ideas. Read first, tick when done.

## Decisions

### Architecture and backend

- [[terminal-sessions-and-host-exit]] — 2026-09-08: `/login` field fix = host focus on activation + the ONE sanctioned origin-lock exit (user-initiated http/https `window.open` → default browser) + clipboard-read grant + Ctrl+click OSC 8 links + Ctrl+Shift+V/Shift+Insert paste; launch dialog `Claude · Terminal · Other` with WSL shell / PowerShell (user's call, "alles moet mogelijk")
- [[restart-preflight-standby]] — 2026-09-08: the restart PREFLIGHTS before touching anything (user's call, "bulletproof"): dependency check (never auto `npm install`), always-rebuild `web/dist` into `dist-next`, standby child proven over messages-only IPC (`AI_SM_STANDBY=1`, ready → swap → teardown → go), `422` = refused + untouched; settles the 2026-09-06 open decision; `AI_SM_WEB_DIST_DIR` test seam
- [[backend-restart-same-port]] — 2026-09-06: restart button + "new version" toast/pill/confirm (user's call); `POST /api/restart` = same-port handoff to a detached child (`AI_SM_PORT_HINT`, fallback auto-pick) because the WebView2 host locks the exact origin incl. port; `update.available` = disk newer than process; sessions do not survive (dialog says HISTORY keeps them); socket-passing over IPC and a `restart` end reason rejected
- [[log-everything]] — 2026-09-06: `server.log` logs everything by default (debug, `AI_SM_LOG_LEVEL`), boot banner with commit + web build, `POST /api/client-log` ships browser lines, byte COUNTS never bytes, trust-keyed refusal budget (unauthenticated access log was a log-wipe primitive), rotate-failure truncates instead of wedging; sendBeacon rejected (no auth header)
- [[session-history-resume]] — 2026-09-06: cumulative `history.json` + injected `--session-id` → real per-conversation `--resume` (reverses the 2026-07-19 fiction cut); history grouped per project folder; launch dialog cut to a short form with an Effort select; transcript prune is one-directional (realpath + per-cwd dir must exist). Known limit: `--continue` launches can't be pinned
- [[lifecycle-bound-backend]] — sessions die with the app (presence WS + grace timer); its crash-safe journal superseded 2026-09-06 by [[session-history-resume]]
- [[auto-port-discovery]] — backend picks a free port and publishes runtime.json (port + auth token); amended 2026-09-22 (B6 D5): the last port is remembered in `last-port.json` and tried first, auto-pick is the fallback
- [[vanilla-ts-vite-frontend]] — no UI framework; vanilla TS + Vite around imperative xterm.js
- [[web-app-inside-wsl]] — why the app is a web app served from WSL, not a Windows-native Electron app
- [[detached-backend]] — partially superseded by [[lifecycle-bound-backend]]: setsid detachment from the launcher stands; window-close survival (and the systemd user service plan) reversed

### Launcher, installer and release

- [[in-app-update]] — 2026-09-09: notification + ONE Update button (user's call, like other apps): backend checks GitHub releases (ETag, silent failures), downloads Setup + SHA256SUMS, verifies BEFORE anything runs, starts the Setup silently via interop; Setup reuses install-info.txt, closes the host cleanly, relaunches; sessions end like Restart; rejected: host-side updater, in-place swap, auto-install
- [[installer-and-self-contained-bundle]] — 2026-09-08: the app becomes a product (user's call): self-contained WSL bundle (pinned Node + node-pty built on 22.04, no Node/git/build tools for users), Inno Setup unsigned per-user installer, explicit opt-in for every third-party install, no-WSL = explain and stop, repo goes public; backend "installed mode", `app/<version>` + `current`, updates = newer Setup.exe; phases A–D
- [[release-build-and-launcher-derivation]] — 2026-09-08: GitHub Actions release of the native host zip on `v*` tags (`gh release create --verify-tag`, re-run rewrites notes), `ci.yml` typecheck+build+test, SHA-pinned actions, `permissions: {}`; launcher derives distro + repo path from `\\wsl.localhost\<distro>\<path>\launcher` (env → location → defaults, same allow-list, invalid = FAIL not fallback); app never packaged, version = tag; repo visibility OPEN (user)
- [[native-webview2-host]] — 2026-07-23: fix the Edge-logo taskbar icon by bringing a lightweight WebView2 host forward (not Tauri); cheap AUMID/shortcut fix proven structurally impossible here. **+2026-07-24: dark window chrome via DWM caption/text/border colors — user chose this over a frameless window with a custom title strip (still available later)**
- [[thin-windows-launcher]] — health-check → start via wsl.exe → Edge --app window; Tauri is the upgrade path

### UI and Nocturne

- [[b6-settings-live]] — 2026-09-22: B6 (user): any New-session card can be hidden, at least one stays; the notifications row dropped until C1; `Reopen tabs on start` off = only a NEW backend run starts on Home (keyed on start time); `Check for updates` = `POST /api/update/check` now + the answer on the page; the backend keeps its last port (`last-port.json`, auto-pick = fallback) so the layout survives an app start; defaults: prefs keys `behaviour` + `tools` (never `defaults`), confirm-end = the armed two-step on every door, follow output = scroll on every write, the Keyboard page from one shared rows module
- [[b4-editor-live]] — 2026-09-22: B4 (user): unsaved text never dropped without a question (`Discard` / `Keep editing`, `beforeunload`; backend grace = the one silent door); editor tabs survive a reload (text does not); a clean tab follows its file on disk (5 s, `if=<stamp>`); a save onto a changed file is refused (409) with `Overwrite` / `Load from disk`; defaults: Ctrl+S in the field, 1 MiB text only, eol + BOM preserved, SHA-256 stamp, in-place write on one fd, `files-mock.ts` deleted
- [[b3-commits-live]] — 2026-09-21: B3 (user): the Commits tab = the last 10 commits + `Show more`, rows with date and author; `Open on GitHub` for github.com remotes only, absent otherwise (the page builds the URL from a gated `{owner, repo}`); defaults: HEAD history, merges vs first parent, no e-mail, pinned paging, first ten blocks unfolded; known limit: the whole repository's object store is readable through `commit-diff`
- [[b10a-multi-select-and-delete]] — 2026-09-20: B10a (user's ask after B10): multi-select in the Files panel (Explorer semantics) + PERMANENT delete with one confirmation per action (`Delete 3 items from src? This cannot be undone.`); no trash; anchors never deletable; the app's first delete primitive
- [[b10-file-copy-and-clipboard]] — 2026-09-20: B10 scope = real upload (drop / paste / `Copy files here…`) AND copy-to-clipboard via the native host as the LAST phase; folder conflict with one choice per drop: Replace = merge, Keep both = `web (2)`, Skip = whole folder; limits 2000 files / 1 GiB per drop on top of A9's 50 MiB / 200 items, refused up front
- [[b5-tools-keys-and-shells]] — 2026-09-18: open decisions 4 + 6 settled (user): API keys server-side in `keys.json`, injected into the tool's child env only (Codex signs in inside the terminal); Command Prompt via interop with a server-injected `/k pushd <windows path>` (allow-listed), no `pwsh.exe`; absent tools = inert `Not installed` cards from a PATH probe; Start from = Claude per-id, others last-only; per-tool permission/effort mapping recorded; § 5 (2026-09-20): ending a session = SIGHUP → SIGTERM → SIGKILL on the process GROUP, rung skipped only when the group is gone (Gemini CLI ignored HUP/TERM and kept the key in its env)
- [[b7-background-agents-source]] — 2026-09-22: B7's open decision settled (user): the Background agents table reads Claude Code's own subagent transcripts (`~/.claude/projects/<slug>/<session-id>/subagents/`) found through the payload's `transcript_path` in the B1 snapshot; hooks and dropping the part rejected; B11 follow-ups decided the same evening (switch default off, Working/Waiting for you colours, ≤ 4 running rows + counts); shape = untrusted-path boundary, 2 s poll, incremental reads, billed-total tokens, 15 min stale rule, 8/3 cap
- [[b11-session-turn-readout]] — 2026-09-22: B11 decided (user): Working = green pulsing, Waiting for you = amber still (from the session's own transcript), BEL unchanged; agents table behind a Settings switch default OFF; ≤ 4 running rows + `+N working`, ≤ 1 finished row + `+N finished`; pulse only where the turn is known, waiting never notifies
- [[pane-status-bar-data-source]] — 2026-09-16: open decision 2 settled (user); implemented 2026-09-17 in B1 (app-id-keyed snapshot via a fourth script argument, `paneBar` switch, `Session time` row); default flipped the same day by the user: the bar ON, Claude's line OFF: B1's pane status bar reads the payload Claude Code already hands `server/statusline.mjs` (per-session snapshot cache in the data dir → session info → same checklist as Settings → Status bar); Active skill dropped (no source); values shown twice unless Claude's own line is switched off; hooks and "Claude's line only" rejected
- [[terminal-colours-shape]] — 2026-09-13: open decision 10 settled (user): presets + custom ground/text; ground + text only; terminal only, status colours never themed; Settings page only (A7 visual, B9 live)
- [[nocturne-full-switch]] — 2026-09-10: full switch to the v3 "Nocturne" handoff (user's call); Legacy UI = git tag `legacy-ui`, no toggle, popover dies in A8; plan `.claude/plans/PLAN-NOCTURNE.md` A1–A8/B1–B8; A1 decisions: alias layer over `app.css`, xterm palette = exact hex of the oklch semantics, Inter self-hosted (sanctioned), icon from `app-icon.svg`, host DWM colours; **A4 decisions**: no command preview, labels + one permissions info popover, "Other" card, Bash naming, top-anchored modal; A4b + A5 + A6 landed 2026-09-13
- [[no-code-in-ui-copy]] — 2026-07-25: no commands/flags/config names in the GUI; modes read "Always ask / Auto-approve edits / Read-only planning / Never ask", argv preview → readable summary; UI stays English; custom-command field exempt
- [[launch-dialog-custom-escape-hatch]] — 2026-07-20: launch dialog gains a `custom · any command` chip (user's call over claude-only), preserving configurable command + args in the GUI
- [[handoff-design-primary]] — 2026-07-20 flip: the user's hi-fi handoff is the primary design source; bottom tab strip; modal launch dialog
- [[anti-slop-design-direction]] — terminal-derived visual identity; hard reject list for generic AI aesthetics

### GitHub

- [[github-token-paste-path]] — 2026-07-25: an "add token" path beside the device flow (user's request), persisted with a remember-toggle; design gate ran BEFORE any code — storage ceiling is 0600 + discipline, and the real win is a fine-grained token with an expiry. **Implementation queued**
- [[owner-qualified-clone-paths]] — 2026-07-25: GitHub-list clones land at `<home>/projects/<owner>/<repo>` (settles the open decision); kills the same-basename 409 *and* the wrong-project button; rejected adding `remote` to `Project`
- [[github-integration]] — 2026-07-23: app can create projects + connect to GitHub (OAuth device flow, user's call); v1 = full create-local / clone / create-repo; token stays server-side

### Process

- [[repo-layout]] — 2026-09-20: where things live and how they move (user's calls): `/restructure-repo` first; docs now, code after B10; work log = `log/<YYYY-MM>/<area>/`; decisions + knowledge flat with a theme-grouped INDEX; planning has one shape (master plan + status table, part specs that never move, `.claude/plans/README.md`); both layouts enforced by tests; move maps of batches 1–2; plan `.claude/plans/PLAN-RESTRUCTURE.md`
- [[agent-team-and-dev-flow]] — the subagent roster, strict lanes, and the develop→review→fix loop

## Knowledge

### Backend and PTY

- [[backend-from-claude-code-taints-pty-env]] — 2026-09-17: a backend started from a Claude Code terminal passed `CLAUDECODE` / `CLAUDE_CODE_CHILD_SESSION` … into every PTY, so app-launched claudes ran as nested children with transcript saving OFF (no resume); `ptyEnv()` now strips the parent markers by name, never the `CLAUDE_CODE_*` prefix (user config)
- [[fifo-open-blocks-main-thread]] — 2026-09-17: a planted FIFO hangs a synchronous `openSync` before `isFile()` can refuse it (froze the backend AND the status-line script); `O_NOFOLLOW` is not a FIFO defence — open with `O_NONBLOCK` too, test with a real `mkfifo`
- [[etag-cache-verdict-not-payload]] — 2026-09-10 incident: the release check cached the VERDICT ("not newer") beside GitHub's ETag; after a downgrade a 304 re-used it and the Update button never appeared. Cache only what the validator covers, derive version-relative decisions at use time, rename the field so old files fail the read; same shape: ctime-order tests assumed a monotonic clock (`proveNewer`)
- [[path-normalization-delete-primitive]] — 2026-07-25: a cleanup that "removes only what we created" wipes a pre-existing directory when the path carries `..` — `resolve()` is lexical, the kernel is not; one normalization is worthless if one consumer still reads the raw string
- [[pty-exit-data-race]] — **FIXED 2026-07-25**: a session's LAST output vanished at exit — libuv fabricates an EOF on POLLHUP and never re-reads, so the kernel's remaining bytes are dropped (NOT node-pty ordering, my first guess was wrong). Tell: stream emits `'end'` instead of `'error' EIO`. Reproduce by stalling the READER, not by CPU load
- [[pty-requirements]] — why every session needs a real PTY and what breaks without resize propagation

### Security

- [[git-read-calls-run-repo-config]] — 2026-09-21: "read-only" git runs what the REPOSITORY configures — a partial-clone config + one missing object makes `log` / `diff` / `rev-parse` run `uploadpack` / `core.sshCommand` (`GIT_NO_LAZY_FETCH=1`), `log.showSignature` runs `gpg.program`, textconv runs on any patch; `log -1 <hash> -- <path>` answers with an ANCESTOR's diff (`--no-walk`); `show` implies `-p`; every "not executed" proof needs its non-vacuity half
- [[wsl-0600-not-a-boundary]] — 2026-07-25 VERIFIED: any process running as the WINDOWS user reads every 0600 file in the WSL data dir through `\\wsl.localhost\` (the 9p server runs as root) — read the live app token from PowerShell, no elevation; no keyring exists in this distro either
- [[localhost-security-model]] — the drive-by-web-page threat; token auth, Origin/Host checks, argv spawning

### Frontend and terminal

- [[alias-layer-migration-guards]] — 2026-09-14: deleting a token alias layer = survivors up first (grep TS for runtime-read `--` names: `--fs-term`), regions in parallel, guard last (marker gone, every var() declared, every token read, retired names banned, floors); resolved-value diff per selector is the no-visual-change proof; class-parity scanners lie on `@media`-only mentions and template-literal names; `.modal-scrim` is a keys.ts contract, not a style
- [[slots-migration-lessons]] — 2026-09-15: sessions→slots rename (tsc = checklist), rebuild key = layout not contents, dims ladder ORDER, prune shared text at the slot not the view (mergeViews transit), Home-at-0 needs a non-empty strip test, raw NUL bytes blind grep, HTML5 drag channel kept free, allowlist key nothing acts on = stolen
- [[v3-parity-vs-user-decisions]] — 2026-09-14: a handoff-parity finding never outranks a recorded user decision (the New-session subtitle came back reworded past a literal ban list; scope re-review caught it); ban paraphrases too; reviewers can be wrong on arithmetic (`--toast-bottom`); a screen no plan part owns (Projects drawer) reaches the gate on Legacy literals
- [[parallel-halves-one-stylesheet]] — 2026-09-13: two developers on one app.css by block region works; seams = shared rule deleted early (whole-file parity test), child dialogs nobody owns (folder picker), mocks hand-copying tables, fake-dom click() ignores disabled, error lines skipped by copy passes
- [[hidden-grid-and-mock-coherence]] — 2026-09-13: covering the pane grid = `grid.hidden` + refuse ALL `render()` work (the reconcile branch built xterm on display:none and resized a live PTY to 80x24) + replay on return; chords stand down; open-before-close for one layout pass; code surfaces without ligatures; mock numbers COUNTED from drawn rows (renderer-vs-header test, per-path seed); missing datum = note not content; unresolvable screen keeps its exit; history ms-tie flake
- [[files-panel-focus-owner-trap]] — 2026-09-13: an always-open panel must never match the screen-level `OPEN_FOCUS_OWNER_SELECTOR` (shared `drawer` class silently killed the terminal refocus); reuse classes for CSS not semantics; mock data over a real name needs one quiet marker line; persisted wish needs its own title; `tests/fake-dom.ts` shared DOM double
- [[xterm6-ground-and-font-race]] — 2026-09-13: xterm 6 `.xterm-viewport` is an empty div painting `#000` over `.xterm`'s padding (repaint with the ground token; test scans the installed xterm.css for new black surfaces); load the mono face BEFORE the first draw or the cell width (cols/rows) is the fallback's; OptionsService drops equal-value writes (trailing-space nudge); a throwing `check()` ≠ loaded; a waiting boot gate must be a boot-panel row
- [[google-fonts-subset-trap]] — 2026-09-10 incident: Google's latin-EXT woff2 has NO ASCII; "font loaded" ≠ "font renders" — prove glyphs (fontTools cmap / measureText), pin font assets by size+sha256; xterm.js colours must be hex/rgb, never oklch/color-mix
- [[localstorage-origin-port-churn]] — auto-picked port = new origin per backend run = localStorage resets; durable prefs belong server-side; amended 2026-09-22: the port is sticky, the bucket dies only on a fallback port or a cleared profile
- [[frontend-terminal-quirks]] — AltGr vs Ctrl+Alt chords, xterm detached-mount trap, attention poll latency, token rotation

### Windows, WSL and installer

- [[inno-setup-local-compile]] — 2026-09-09: compile the `.iss` from WSL with a PORTABLE Inno Setup (`/CURRENTUSER /PORTABLE=1`, no elevation) instead of a 6-min CI loop; measured Inno 6.7.1 facts (`SuppressibleMsgBox`, `DisableWelcomePage` default yes, silent mode simulates Next, no `LoadStringsFromFileUTF8`)
- [[inno-setup-ispp-char-literals]] — 2026-09-09: ISPP reads any `[Code]` line starting with `#` as a directive — `#13#10` may never open a line (first ISCC compile on CI failed on it); pinned by test
- [[webview2-focus-links-clipboard]] — 2026-09-08: WebView2 never takes keyboard focus back after an Alt-Tab (`ActiveControl = null; Focus()` on Activated); popups die under the origin lock so OSC 8 links need a ShellExecute exit checked in C#; clipboard-read needs PermissionRequested Allow; PowerShell in a Linux PTY works but blocks on `ESC[6n`; reproduce Claude's login screen with `CLAUDE_CONFIG_DIR`
- [[wsl-interop]] — localhost forwarding, calling Windows binaries from WSL, cold-boot delay; **2026-09-09:** `wsl.exe --` double-expands argv (use `--exec`), `wsl -l -v` is UTF-16LE no BOM (`WSL_UTF8=1` on the Windows child), tarballs over stdin, `\z` not `$`

### Build, tests and repo

- [[wsl-clock-steps-backwards]] — 2026-09-20: WSL2 steps its clock backwards on host re-sync (`dmesg`: "Time jumped backwards"); a lone red test comparing wall-clock stamps (`restart` "frontend rebuilt", `lifecycle` history order, `ended` before `createdAt`) = check dmesg, re-run, never weaken the assertion
- [[author-path-guard-tracked-only]] — 2026-09-16: `tests/no-author-paths.test.ts` walks `git ls-files`, so new untracked files spelling the author's real home pass locally and fail on CI; briefs use `/home/you`, `git add -N` before the suite
- [[dev-machine-setup]] — 2026-09-15: fresh WSL clone needs Node 24 via nvm (`start-backend.sh` sources nvm; node-pty ABI-bound to the installing node), `npm run build`, Playwright Chromium user-level; sudo list (jq, shellcheck, htop, unzip, libnss3/libnspr4/libatk-bridge); dev launcher on the default data dir REATTACHES to the installed v0.3.2 — use `AI_SM_DATA_DIR=~/.ai-session-manager-dev`
- [[vite-config-cwd-trap]] — 2026-09-08 incident: `vite build` run from inside `web/` finds no config → bare `__BUILD_ID__` → ReferenceError in a `.then(ok, err)` success handler → boot step never settled, page hung on 'token check'; fixes: `web/vite.config.ts` re-export shim, `typeof` read, settle-before-log
- [[test-teardown-late-writer-race]] — **FIXED 2026-09-08 (tests only)**: CI-only ENOTEMPTY on temp-dir teardown — `destroyAll()` returns before the pty `exit` handler logs its `<id> totals:` line into `server.log`, recreating the file mid-`rm`; fix = `destroyAllAndSettle` (wait for the real last log line) + `removeTempDir` (maxRetries backstop); reproduce by pinning to one CPU with spinners, not by re-running the suite

## Log

Entries live at `memory/log/<YYYY-MM>/<area>/` (areas: nocturne, backend, ui,
launcher, github, release, project); the link target is still the filename.

### 2026-09

- [[2026-09-21-repo-restructure-batch3]] — restructure batch 3: one `design/` home (`session-manager/` + `peek-mascot/` still local-only, `archive/handoff-v1/`, `design-mocks/` deleted ahead of B8); "continue" is not a yes to publishing; a peer session appeared mid-batch → stage by pathspec, batch 4 parked
- [[2026-09-22-nocturne-b11]] — **Nocturne B11 LANDED** (Working vs. Waiting for you from the session's own transcript via `turnOfLine` + tail read inside the B7 boundary; pending slug for a folder Claude never opened; ≤ 4 running rows + counts; `paneAgents` switch default OFF; round-robin tick budget; suite 3368→3437): two parallel developers, one fix round (3), security clean ×2, 90 mutants / 4 equivalent, verify-terminal T1–T4 PASS with a real claude session; all subagents on opus from now on (user); Windows check owed; next B8
- [[2026-09-22-nocturne-b7]] — **Nocturne B7 LANDED + user-verified** (`88209b4`, "hij werkt dus wel"; B11 noted: session Working vs Waiting for you + the double agent list) (Background agents table live on Claude Code's subagent transcripts: `transcript_path` through the B1 snapshot, `server/agents.ts` boundary (realpath inside `<CLAUDE_CONFIG_DIR|~/.claude>/projects`, re-checked per poll), 2 s poll with bounded incremental reads (4 MiB/file, 16 MiB/tick), billed-total tokens, `end_turn`/15-min-stale finished, 8/3 cap, themed ink + hairlines (B9 constraint); suite 3258→3368): decision asked first (transcripts over hooks/drop), two parallel developers, one fix round (7: symlink at `<uuid>`, log hygiene, `isLive` gate, `CLAUDE_CONFIG_DIR`, refusal log, global read budget), 81 mutants / 0 live, verify-terminal all PASS; lessons: realpath the directory OPENED not the input, per-file caps are not per-tick caps; Windows check owed; next B8
- [[2026-09-22-nocturne-b9]] — **Nocturne B9 LANDED + user-verified** (`6b1afc1`, "alles werkt") (Terminal colours live: the Settings page paints every terminal through `ui/theme.ts`, `prefs.theme = { ground, text }` hex pair with the Legacy index shape still read, Nocturne = the absence of an override, terminal-only surfaces at the CSS seam, debounced + serialised server write with `adopt` on the dialog's re-read; suite 3223→3258): one developer, one fix round (a reopen race that would have clobbered the durable pair; `updatePrefs` wiping the bag on a failed GET, pre-existing), 18 mutants, verify-terminal via the reused B6 CDP harness; next B7 (data source open) or B8 on the user's word
- [[2026-09-22-nocturne-b6]] — **Nocturne B6 LANDED + user-verified** (`7f9e7c1`, "alles werkt") (Settings live: `Tools` toggles hide New-session cards, three `Defaults` rows live, the Keyboard page from the shared `shortcuts-rows.ts`, `Check for updates` = `POST /api/update/check` with the answer on the page; D5 the sticky port `last-port.json` so the tab layout survives an app start): three developers + three gates in parallel; reviews found a false spec claim about the confirm's mode, two D3 blockers (unawaited runtime fetch, restart reload = new run) and that the switch was invisible under per-port localStorage
- [[2026-09-22-nocturne-b4]] — **Nocturne B4 LANDED + user-verified** (`6d56028`, four-tab cap `39cbfbb`, suite 3033→3127): the editor reads, saves (in place, one fd, SHA-256 stamp, 409 → `Overwrite` / `Load from disk`), follows its file on disk (5 s, `if=<stamp>`), asks before dropping unsaved text (every door + `beforeunload`) and keeps its tabs across a reload; `files-mock.ts` gone; reviews: the spec's 4× body cap was 6× (C0 escapes), a unix socket 500'd, hard links defeat the realpath boundary (known limit, pinned), typing during a write was marked clean, a follow answer could overtake a save; gates 106 + 10 mutants, no real defect
- [[2026-09-21-nocturne-b3]] — **Nocturne B3 LANDED** (`1096e81`, suite 2915→3002): Commits tab, commit view and diff tabs on the real repository (`server/git-log.ts`: `/api/git/commits` · `/commit` · `/commit-diff`, 10 rows + `Show more`, `Open on GitHub` for github.com only); security review found code execution through a partial-clone lazy fetch (also in B2's shipped Changes tab) → `GIT_NO_LAZY_FETCH=1`; scope review found `log -1 -- <path>` answering with an ancestor's diff → `--no-walk`; gates 102 + 21 mutants, one real defect (body cap halved an emoji); the user's Windows check found three more the same night (a long message filled the view, the view outlived its session, a UTC date spelled `Z` kept CI red) — fixed, user-verified
- [[2026-09-20-repo-restructure-batch2]] — restructure batch 2: plans into `.claude/plans/` (+ `nocturne/` part specs, ids spelled like the master plan), status TABLE on top of `PLAN-NOCTURNE.md`, conventions in `.claude/plans/README.md`, `tests/plans-layout.test.ts`; `landed/` folder rejected after counting 45 code/test citations; 50/50 code lines proven pure path substitutions
- [[2026-09-20-nocturne-b10a]] — **Nocturne B10a LANDED** (`0293848`, suite 2765→2908): many selected rows (Explorer gestures, arrow keys new) + permanent delete with one confirmation; `POST /api/fs/delete` with the boundary on the PARENT (symlink unlinked never followed), anchors refused as target AND container (review found the parent-of-a-project gap), async `rm` (rmSync froze every PTY); focus-on-body after a keyboard delete fixed; gates 12 + 41 + 39; user-verified on Windows same evening
- [[2026-09-20-nocturne-b10]] — **Nocturne B10 LANDED** (`5779d23`, suite 2600→2765): the A9 drop dialog copies for real — walk → limits (200 items / 2000 files / 1 GiB) → one conflict question (Replace = merge, Keep both = `web (2)`, Skip) → `PUT /api/fs/upload` per file under the home/project boundary (per-mkdir realpath + data-dir re-check, `.part` + rename/link), `Hide` while copying, quiet per-file client log; `Copy` in the row menu via `GET /api/fs/winpath` + the host's `copy-files` string channel (`SetFileDropList`, origin-locked, shape-checked, class-only logging, .NET 4.7.2 path handling); gates 75 + 42 mutants; user-verified on Windows same evening ("alles werkt")
- [[2026-09-20-repo-restructure-batch1]] — `/restructure-repo` skill + batch 1: 48 log entries into month/area folders with zero link edits, INDEX regrouped, README layout map; two sessions in one checkout coordinated by message; batches 2–4 after B10
- [[2026-09-20-nocturne-b5]] — **Nocturne B5 LANDED** (dialog live for Codex/Gemini CLI/Grok/Zsh/Command Prompt: `GET /api/tools` probe → inert `Not installed` cards, `keys.json` → per-tool child env, `cmd.exe /k pushd`, Claude per-id resume, per-tool argv byte-exact; suite 2480→2594): 18 Sept phases + reviews + 159-mutant gate, limit-killed at verify; 20 Sept verify V1–V7 PASS, Gemini ignores HUP/TERM → kill ladder on the process group (auditor: follow the group, sweep at shutdown), 29-mutant gate; user-verified on Windows same day (Codex + Gemini CLI work)
- [[2026-09-17-nocturne-b1]] — **Nocturne B1 LANDED** (pane status bar on the statusline snapshot: fourth script argument → `statusline-snapshots/<appId>.json` write-on-change → `server/telemetry.ts` watcher, untrusted parse → `SessionInfo.telemetry` → the same checklist as Settings → Status bar with two switches + `Session time`; suite 2418→2479): Phase 0 + two parallel developers, 1 fix round (8), 37 mutants, FIFO-open bug found by the gate, verify-terminal with a real claude session; same day: PTY env strip of Claude Code's child markers, default flipped to bar-only (user); user-verified ("alles werkt"); next B5
- [[2026-09-16-nocturne-b2-a9c]] — **B2 + A9c LANDED** (real file browser: `/api/fs/entries` / `/api/fs/create` / `/api/git/changes` under a home+projects realpath boundary, git hardened; live panel + Changes tab; New file/New folder from row + root menus; suite 2231→2418): decisions (real over mock, whole-repo changes, anchor list, Home=home), lean dev-flow rules, lessons (fsmonitor executes on status, WSL2 clock jumps, mutation gate vs readers); owed: Windows check; next B1
- [[2026-09-16-nocturne-a9b]] — **Nocturne A9b LANDED** (select a folder → files paste lands there from anywhere incl. a focused terminal; Nocturne row context menu `ui/context-menu.ts`, Copy/Paste disabled until B10; suite 2093→2231): 3 phases, 3 fix rounds, 123 mutants; lessons: author-path guard scans tracked only, fake DOM SVG≠HTML, ellipsis on flex, Menu-key contextmenu follow-up; owed: Windows check; next B1
- [[2026-09-15-repo-security-and-backlog]] — 2026-09-15 night: CI red ×2 after the mascot merge fixed (handoff skip-guard, env-dump race), `workflow_dispatch` on verify, **security tab complete** (SECURITY.md, private reporting, CodeQL incl. C# traced build on Windows, Dependabot alerts + security updates), 12 test-only alerts dismissed by the user; backlog +4 sections (CLI compat guard, engineering debt, for-other-users, optimisation round after feature-complete); honest score 6.5 overall / 8.5 vs vibecoded; competitor field noted
- [[2026-09-15-nocturne-a10b]] — **Nocturne A10b LANDED** (editor pane with file-tab strip: one `editor` slot kind with stable `e:<n>` key, add-or-raise, filetab drags split/move, chords pgup/pgdn/m; suite 1973→2043): parked+resumed on `wip/a10b`, NUL bytes caught again, 22 mutants; verify-terminal 19/19 PASS via CDP (claude at 76x16 with an editor focused); next = user's Windows check, then A9b design on request
- [[2026-09-15-peek-mascot-phase1]] — **Peek mascot phase 1 LANDED (merged to main 2026-09-15 night)** (user's own 1:1 design: standalone count-driven `web/mascot.html`, transparent ground, `?demo`; not wired; suite 1973→2022, 12 mutants): feasibility for C1 = attention count + a TopMost/NOACTIVATE transparent WebView2 tool window in the host; exclusive-fullscreen limit; open decisions 12–16; worktree/handoff/virtual-time lessons; CI red ×2 after the merge (handoff read at module load → skip-guard; `env > file` poll race in restart.test.ts → atomic rename), run 62 green
- [[2026-09-15-nocturne-a9]] — **Nocturne A9 LANDED** (drop files/folders from Explorer, visual + mock: window-capture `Files` listeners with preventDefault-first + boot guard, cursor ghost, folder-row/panel/pane/empty-tab targets named like the Files header, `fd-` conflict/copy/result dialog with honesty lines, twins = strip button + ctrl+alt+c + paste; suite 1867→1973): 3 briefs, 2 fix cycles (11 findings), scope ×3, 36 mutants, verify via CDP `Input.dispatchDragEvent` with real files; Windows Explorer drag + clipboard paste owed to the user; next A10b
- [[2026-09-15-nocturne-a10]] — **Nocturne A10 LANDED** (editor column → file/diff PANES in the tab strip, slots model, fixed `Home` tab, pointer drags for files, ctrl+alt+enter/w; + Files panel without session, one left panel, A11 Commits-at-Home; suite 1763→1867): 3 phases (model, grid+tabs ∥ drag+keyboard), 1 fix cycle (7 findings), scope ×2, test ×2 (20 mutants), verify-terminal 9/9 via CDP; next A9
- [[2026-09-14-nocturne-a8]] — **Nocturne A8 LANDED, Track A complete** (Legacy alias layer deleted: 558 refs → 0, tokens.css 143 tokens/8 sections; theme popover + `.btn` family gone; boot card, shortcuts overlay `sc-`, toast `ut-`, restart confirmation `rs-` in the Nocturne idiom; Projects drawer onto v3; suite 1694→1745): 3 phases, 2 parallel developers, 3 fix cycles, scope ×3 / test ×2 / compare ×2 / janitor; verify-terminal 1–9 PASS via CDP with 60 screenshots; the subtitle revert lesson; next Track B (B1)
- [[2026-09-13-nocturne-a7]] — **Nocturne A7 LANDED** (Settings v3 left-nav with five pages incl. the NEW Terminal colours page (mock, decision 10), Add-a-project dialog + folder picker onto Nocturne; suite 1569→1694): two parallel developers by CSS region; 2 fix cycles, zero MUST-FIX; picker gap found by review; A8 size warning (~406 alias refs); user-verified on Windows 2026-09-14; next A8
- [[2026-09-13-nocturne-a6]] — **Nocturne A6 LANDED** (commit view replacing the pane area + editor column at 54 %, mock content; suite 1457→1569): grid hidden not disposed, single top guard after a MUST-FIX on the reconcile path; editor LEFT of the panes per v3 HTML (README says right); 3 honesty lines; 2 fix cycles + 1 inline (unseeded renderer); pre-existing history sort flake fixed; next A7 after asking decision 10
- [[2026-09-13-nocturne-a5]] — **Nocturne A5 LANDED** (Files panel + Commits list mocked, Sessions panel v3; suite 1358→1457): resizable 200–520 panel as flex sibling, badges/pulse, `leftPanel`+`filesWidth` in the v2 bag; MUST-FIX caught: panel as `.drawer` disabled terminal refocus; orchestrator settled mock-marker line, persistence, meta place; verify-terminal 1–9 PASS with panel open; next A6
- [[2026-09-13-nocturne-a4b]] — **Nocturne A4b LANDED** (terminal ground + font load fix; suite 1320→1358): black band = xterm 6 viewport div; `font-ready.ts` + fourth boot row `terminal font` + `reloadFont()`; review caught the un-caught boot await, the blank-page wait, the silent `check()` throw and the IME popup riding along; verify-terminal 1–9 PASS twice; Windows look owed; next A5
- [[2026-09-10-nocturne-a4]] — **Nocturne A4 LANDED** (New session dialog; suite 1259→1320): user's calls = no command preview, labels + ONE permissions info popover, "Other" as 6th tool card; 3 inert tools + 2 inert shells until B5; review caught a centred modal moving the grid under the pointer (→ top-anchored) and Bash/WSL-shell naming split; fake-DOM harness runs the real dialog (1/23 → 23/23 wiring mutants)
- [[2026-09-10-copy-chord-add-folder]] — **copy + add-existing-folder FIXED** (user reports from the A2 test; suite 1219→1259): Ctrl+Shift+C / Ctrl+Insert copy only with a selection (scope: four chords now); fs/list `empty` → non-empty folder = "Add this folder", empty = create + git init; probe race, dup guard; any-403-is-auth-loss bug fixed (`isAuthFailure`)
- [[2026-09-10-nocturne-a3]] — **Nocturne A3 LANDED** (pane card/header 38/status bar Model-Mode-Time/agents parked/empty state; suite 1189→1219): verify-terminal 1–9 PASS via CDP (9 without a prompt); Time drops on exit; `--color-ink-on-danger` primitive; owed: copy-chord + add-existing-folder before A4
- [[2026-09-10-nocturne-a2]] — **Nocturne A2 LANDED** (top bar 48, tab strip 32, statusline 26, copy rules app-wide; suite 1179→1189): Theme + `?` buttons gone (scope doc updated: shortcuts via statusline button/`?` key), statusline counts alive sessions, uptime `2h 15m`; separator-ban test; leftovers → A3/A5/A8
- [[2026-09-10-nocturne-a1]] — **Nocturne A1 LANDED** (tokens, Inter, icon, host colours; suite 1165→1179): alias layer recolours Legacy chrome; orchestrator caught the latin-ext Inter (no ASCII); 12 scope findings → 7 fixed, rest A2/A8 notes; Windows checks owed; handoff dir is LOCAL-only (`.git/info/exclude`) → parity test uses a committed fixture; next A2
- [[2026-09-10-update-check-cache-fix]] — **"no Update button" FIXED** (`1cdb766`, dev-flow 2 cycles, mutants 8/8, suite 1160→1165): cache holds the latest release DESCRIPTOR, `offer()` derived per call, `gateRelease` deleted; retention tests prove ctime order; stale cache deleted for the user, v0.3.2 = the button's target
- [[2026-09-09-in-app-update]] — **phase E in-app updater SHIPPED**: release check (ETag, gated, constructed URLs), download + inline SHA-256 + manual redirect allow-list, `%TEMP%` staging + `run-update.ps1` (re-hash, exit 0/2/3), flow B installer (`host\next`, `WizardDirValue`, `SetupMutex`), one-dialog UI; review caught log flood, lifecycle hole, lost diagnostics, per-reason dismiss, held-flight deferral, restart-wipes-download; mutants 13/13, zero non-loopback connects proven
- [[2026-09-09-installer-phase-c-d]] — **installer phases C + D SHIPPED, v0.2.0 RELEASED** (user tested the Setup on Windows: "alles okey"; first installable release): five-job `release.yml` (bundle on 22.04, installer via ISCC, release needs all four, one re-hashed SHA256SUMS), `verify / linux bundle`, one NODE_VERSION per file, README Install = Setup.exe; go-public scrub (94 lines → 0) + `no-author-paths` guard; dispatch = test build for the user before v0.2.0; history keeps the old e-mail/paths by user choice
- [[2026-09-09-installer-phase-b]] — **installer phase B + UI slice SHIPPED**: `launcher-config.json` precedence with EMPTY defaults, one `\z`-anchored allow-list shared with the helpers, Inno Setup wizard (never elevates, consent default OFF, `WslArg` guard, `{sys}` powershell), const `sh -c` unpack over stdin with `mv -T` swap + retention + live-dir guard; measured: `wsl.exe --` double-expands → `--exec` mandatory; review caught live-dir rename, ctime-tie flake, WSL 1 in `/SILENT`, prune word-split; suite 964→1026; user: vault public, noreply commits, flip by orchestrator, tag after Windows test
- [[2026-09-08-installer-phase-a]] — **installer phase A SHIPPED**: backend installed mode (`bundle.json`, `current`-symlink update signal, restart verifies the target bundle and spawns its own node), `scripts/build-bundle.sh` (SHA-verified pinned Node, node-pty built with it, smoke-booted, 58 MB), physical-cwd start script; review caught log-line forgery via `Date.parse`, a pill that lit on broken targets, a self-verifying mirror seam, every-errno-is-a-dev-clone; suite 908→964
- [[2026-09-08-cicd-gate]] — **CI/CD gate SHIPPED**: reusable `verify.yml` (check+test) called by `ci.yml` and `release.yml`, publish `needs: [verify, host]` → a red suite blocks a release; `npm run release -- vX.Y.Z [--dry-run]` refuses to tag until the CI run for that exact commit is green; CI-only flake root-caused ([[test-teardown-late-writer-race]]); review caught a `finally` that swallowed the real assertion, membership-not-order assertions, mutant-surviving newest-run logic; suite 878→908
- [[2026-09-08-github-release-build]] — **v0.1.0 RELEASED**: first-try green release workflow, assets verified round-trip; launcher self-locates (28 interop tests, suite→878); review caught the node-pty "no compile" lie (no linux-x64 prebuild → build-essential prereq), stale-hash-on-rerun, dead-end env hint; lessons: `WSLENV /w` not `/u`, `Add-Type` is not dry; OPEN: repo private
- [[2026-09-08-login-and-terminal]] — **`/login` field FIXED + plain terminal sessions SHIPPED**: PTY proven fine, three host/UI gaps closed; review turned plain-click links into Ctrl+click, dropped the same-origin popup navigation, fixed refocus over an open drawer; PowerShell replay known limit recorded
- [[2026-09-08-bulletproof-update]] — **restart hang diagnosed (unstamped bundle) + "bulletproof update" SHIPPED**: preflight (deps → always-rebuild → standby child over IPC) before any teardown, `422` untouched, swap after ready + revert, `AI_SM_WEB_DIST_DIR` seam; review caught the standby child unlinking the LIVE parent's runtime.json and rewriting history (HIGH ×3), dist swapped before proof, tests rebuilding the repo's dist; user backlog: /login paste, plain terminal, GitHub release exe
- [[2026-09-06-restart-and-update]] — **restart button + "new version" toast/pill/confirm SHIPPED** (same-port handoff; suite 612→682): review caught a bare teardown that could leave a live pid with no listener, git hashes rendered in toast copy, a 1-in-4 flake (sleep vs setImmediate) and two vacuous e2e assertions; refuted the `/health`-flood worry; open: build `web/dist` on update?
- [[2026-09-06-log-everything]] — **log-everything phase SHIPPED** (suite 546→612): stale-backend incident diagnosed (UI 12:14 vs backend 10:49 — not a history bug); review caught a banner credential leak, client-side query values re-leaking what the server redacted, dead runtime fields, an unauthenticated log-wipe primitive (fixed twice: 4xx metering, then trust metering), `errorFrames` defeated by `\n    at ` in a body, a wedging rotate, two vacuous tests
- [[2026-09-06-history-and-lean-launch]] — **history + real resume + lean launch dialog SHIPPED**: journal/previous → `history.json`, `--session-id`/`--resume`, per-project folders, Effort select, no explanatory copy; reviewers caught a raw-cwd prune that deleted real conversations (realpath + dir-must-exist fix), a relaunch/debounce race, a label keyed on the wrong field; suite 508→546. Open: drop the Continue checkbox?, backend restart needed for the user

### 2026-07

- [[2026-07-26-statusline-phase]] — **statusline phase SHIPPED**: per-pane strip replaced by Claude Code's native status line (per-session `--settings`, script re-reads prefs → live toggles ~2 s, no restart); settings panel cut to status-line config only (2026-07-20 "decided four" REVERSED); usage % real via rate_limits; mode item = launch mode (payload has no live mode — known limit); cache-poison sanitize + boot wipe from review; suite→508
- [[2026-07-25-token-path-RESUME-HERE]] — pasted-token path shipped (`84da3f0`, suite→533) with the security gate published BEFORE the code and audited against itself; caught a strip that lied in the user's own config, two measured log leaks, and two mutually-shadowing tests. Carried the statusline brief (executed 2026-07-26)
- [[2026-07-25-ui-copy-and-clone-paths]] — plain-language UI copy pass (no commands/flags in the GUI, user's call) + owner-qualified clone paths; the design-delta turned out to be 2 cosmetic items; gate caught a summary that could drift from the spawn, a proven directory-wipe primitive (+ a pre-existing twin), and a test vacuous for 4 of 6 consumers; suite 396→462
- [[2026-07-25-pty-tail-rescue]] — the lost PTY tail FIXED via dev-flow (suite 391→396); root cause was libuv, not node-pty. Reviewers caught a comment asserting an invariant the code didn't implement, and twice turned an inherited guarantee into a local one. node-pty pinned exactly; `/verify-terminal` live pass still open
- [[2026-07-24-github-hardening]] — test-hardening via full dev-flow, suite 288→391: `github-model.ts` extraction (clock injected), `AI_SM_GITHUB_API_BASE` loopback-only test seam, `redirect: 'error'` on token-bearing calls. Lessons: an in-process seam can't reach an out-of-process server; "unavoidable coverage gap" was refuted by a `git` double on PATH; a runtime-inherited guarantee isn't one. Found (not caused) [[pty-exit-data-race]]
- [[2026-07-24-dark-window-chrome]] — white native title bar FIXED: DWM caption/text/border colors from the CSS tokens on the WebView2 host (user picked this over frameless); verified by compiling on Windows + a `PrintWindow` capture reading `#171D25`; gotcha: use `PrintWindow`, not a screen grab, when the foreground lock blocks activation
- [[2026-07-24-github-build]] — **Phase 2 COMPLETE**: project creation (blank + URL clone + picker) → GitHub OAuth device-flow connection + repo listing → token-auth clone-by-pick + create-repo. Token server-side 0600, never leaked (host-locked clone resisted every exfil bypass); config-driven/dormant until `AI_SM_GITHUB_CLIENT_ID`; security CLEAN every phase; suite→288. **Blocked-on-user: register the OAuth App + set the client_id; live verify-terminal pass**
- [[2026-07-24-status-bar]] — *(strip superseded: removed 2026-07-26 by [[2026-07-26-statusline-phase]])* design intake committed; per-pane terminal status bar SHIPPED (real-or-omit telemetry from git + Claude logs; usage% deferred not faked); suite 162→222; 3 user decisions recorded (status-bar-first, git-init toggle, `repo` OAuth scope)
- [[2026-07-23-webview2-host-shipped]] — WebView2 host shipped + Edge taskbar icon FIXED (user-confirmed); \\wsl.localhost exe-launch regression found & fixed; **guidelines for tomorrow: design reconciliation → GitHub build**
- [[2026-07-23-settings-panel-and-scope]] — settings panel landed via dev-flow (162/162, 2 LOW fixed); Edge-icon cheap fix proven impossible → WebView2-host decision; GitHub scope added; design handoff refreshed (delta reconciliation queued)
- [[2026-07-20-theme-persistence-launcher]] — prefs.json + /api/prefs fix the theme reset; launcher finished with desktop shortcut; suite 120/120; agents-on-opus policy; settings panel un-gated
- [[2026-07-20-r3-launch-dialog]] — R3 shipped: modal launch dialog + custom chip + honest boot panel, launcher-as-tab retired; 92/92 tests, 9/9 verify-terminal; theme-persistence phase queued
- [[2026-07-20-r2-handoff-reskin]] — precedence flip + R2 shipped: handoff reskin, bottom tab strip, theme system; 9/9 verify-terminal; settings panel queued user-gated
- [[2026-07-19-design-handoff-and-r1]] — user's hi-fi handoff triaged (gap analysis, fiction cuts, 2 open decisions); presence ping/pong + /api/runtime landed
- [[2026-07-19-lifecycle-and-pivots]] — lifecycle reversal implemented; steam-blend direction chosen; caveman + model-assignment process changes
- [[2026-07-18-mvp-build]] — backend + frontend + launcher landed via dev-flow workflows; backlog and manual-pass list
- [[2026-07-18-project-setup]] — scope agreed, agent team + dev-flow built, memory vault created
