---
type: log
created: 2026-09-10
updated: 2026-09-10
tags: [log, nocturne, design, frontend, launch-dialog]
---
# 2026-09-10 — Nocturne A4: New session dialog LANDED

Context [[nocturne-full-switch]] (A4 decisions section); follows
[[2026-09-10-nocturne-a3]].

User answered the three v3 conflicts before any code: no command preview;
labels-only permission cards + ONE info button with a short explanation of
all four modes; the custom-command hatch as a sixth tool card "Other".
Model policy unchanged (agents opus, security-auditor fable).

Built (terminal-ui): tool card grid 2 per row (Claude Code, Codex, Gemini
CLI, Grok, Terminal, Other; three inert until B5), Name + Project row,
Model + Effort, Permissions 2×2 cards + info popover (`PERM_HELP`), Start
from select replacing the Continue checkbox, Shell cards Bash · Zsh (inert)
· PowerShell · Command Prompt (inert), footer Cancel / Start session. New
`ns-` CSS block on primitives, `--color-scrim`, hand-drawn info icon.
`composeSpawn` untouched; every pre-A4 state emits byte-identical argv
(live launches read back from `GET /api/sessions`).

Review: security CLEAN. scope-reviewer 3 should-fix — the centred modal
moved the tool grid 78–98 px under the pointer on every Tool switch (the
orchestrator spotted it in the screenshots; a double-click on Terminal
landed on Claude Code), "Bash" card vs "WSL shell" everywhere else, stale
scope doc — plus notes (model label split, a second permission vocabulary
in `server/statusline.mjs`, stale comments). test-engineer found the
developer's tests guarded only `launch-args.ts`: 1/23 wiring mutants in
`launch.ts` caught → wrote a dependency-free fake-DOM harness that runs the
real dialog (`tests/ui-launch-dialog.test.ts`, new pattern for the repo;
192 states through the DOM), 23/23 caught. Fix round: top-anchored scrim
(`max(space-6, 50vh − 317px)`, top identical for every kind), `SHELLS`
label → Bash as the single source, `modelLabel()` at the three render
sites. Re-review: notes only; the test re-gate caught a max-height that
ignored the new top padding (footer could leave the window in the stacked
≤360 px layout) → `max-height: min(88vh, 100%)`, measured 360×860 fits
exactly. Janitor: comments only. Suite 1259 → 1320; 1 fix cycle.

Follow-up (user's ask the same evening): the Shell cards were four across
and "Command Prompt" wrapped to two lines — they now sit two to a row at
the Tool cards' 40 px, and the Permission cards went from 38 to 40 px, so
every card grid in the dialog shares one size; top anchor 317 → 319 px.

Lesson: a modal whose content height depends on a selector inside it must
not be vertically centred — measure the grid's position across states, not
one screenshot per state.

Owed: Windows look in the real WebView2 host; B5 wires the inert cards and
per-id resume; the two permission vocabularies are a user decision
([[BACKLOG]]).
