---
type: log
created: 2026-09-10
updated: 2026-09-10
tags: [log, nocturne, design, frontend, icon]
---
# 2026-09-10 — Nocturne A1: tokens, fonts, icon LANDED

User: "begin met A1". Decision context in [[nocturne-full-switch]]. Tag
`legacy-ui` pushed on `2281465` first.

Two developers in parallel (terminal-ui: tokens/fonts/theme defaults/
index.html; generalist-dev: `make-icon.mjs` rewrite, host DWM constants,
launcher README). Review round: scope-reviewer (12 findings: 3 should-fix,
9 notes) + test-engineer (0 code findings, +12 tests, 16/16 mutants caught).
Orchestrator found the HIGH one itself: the shipped Inter was the latin-ext
subset with no ASCII ([[google-fonts-subset-trap]]). One fixer run, 7 items;
re-review 7/7 resolved; suite 1165 → 1179. CI went red once (`88c6387`): the
handoff parity test read `design_handoff_session_manager/`, which is a
LOCAL asset (`.git/info/exclude`, never committed) — fixed by snapshotting
the 45 primitives into `tests/fixtures/nocturne-handoff-tokens.json`
(`101d5ca`, CI green). Open for the user: commit the v3 handoff like the
v2 `design/` dir, or keep it local? Windows-side checks owed
(host rebuild with new `app.ico` + DWM colours, `make-shortcut.ps1` re-run,
icon cache) — in [[BACKLOG]].

verify-terminal: checks 1, 3, 4 + focus outline passed on a dev backend via
CDP; full 9-check pass deferred to A3 (A1 changes no PTY/WS/resize code).

Next: A2 (shell chrome) on "begin aan A2".
