---
type: log
created: 2026-09-10
updated: 2026-09-10
tags: [log, nocturne, design, frontend, terminal]
---
# 2026-09-10 — Nocturne A3: pane chrome LANDED, verify-terminal 1–9 passed

Context [[nocturne-full-switch]]; follows [[2026-09-10-nocturne-a2]].

Built (terminal-ui): pane card neutral-900 / radius-md / shadow-sm, focused
pane in a split = 1 px accent-700 ring (no dimming); header 38 px (dot, name,
project, state pill Working / Needs your answer / Finished with the exit code
in the title, "Own tab" in splits); terminal card on `--color-term` with the
xterm mount padded inside a wrapper so cols/rows stay exact; status bar under
the terminal from a pure model (`ui/pane-status-model.ts`: Model / Mode /
Time from launch args + createdAt, Claude sessions only, Time dropped once
exited); background-agents renderer parked with `[]` (open decision #7);
drop hint "Drop here to show side by side"; empty state per v3 minus the
grace-countdown line (the backend only counts down when the last window
closes — rendering it would be a fiction); `.pane-scan` gone. No end-session
`×` in the header (v3 has none; tab and drawer keep it).

verify-terminal: all 9 checks PASS on a dev backend via CDP (real BEL in a
hidden tab, resize 202→96 cols matched `stty size` across layouts 1/2/4,
disconnect + hidden-pane survival, stress render bounded, `claude` 2.1.267
TUI drew and took keys; the permission-prompt sub-check was not exercised —
no prompt sent, it would spend the user's account).

Review: scope-reviewer 3 should-fix (alias `--danger-ink` in pane CSS →
new primitive `--color-ink-on-danger`; Time counting after exit; banner
prose in mono) + notes (stale comments, `--panehd-h` lie → 38 px, dead
`--pane-gap`/`--divider-hit`, 15 s DOM rebuild → signature diff);
test-engineer +29 tests (status model matrix, agents empty pin, copy pins),
5/5 mutants caught. Suite 1189 → 1219.

Owed: copy-chord (Ctrl+Shift+C / Ctrl+Insert) and "Add this folder" for an
existing directory — user reports from the A2 test, next flow before A4.
B5 note: `pane-status-model` gates on `isClaudeCommand` and parses Claude
flags; other tools need a per-tool mapping there, not a second gate.
