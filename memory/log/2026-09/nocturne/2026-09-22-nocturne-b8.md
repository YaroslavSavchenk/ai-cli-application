---
type: log
created: 2026-09-22
updated: 2026-09-22
tags: [nocturne, cleanup, pane-header, release, process]
---
# 2026-09-22 — Nocturne B8 LANDED: End session in the pane header, Legacy gone, docs current; v0.4.0 prepared

User: "je mag beginnen aan B8 … En ik wil dat jij ook een knop toevoegen bij
een sessie of terminal rechts boven of links boven om een sessie te sluiten.
Doe dit samen in B8". Asked before the developers started (both answered):
the button ENDS the session (not close-the-pane, not a menu), and the
release is prepared and shown first, published only on the user's word.
Spec `.claude/plans/nocturne/PLAN-B8.md`.

## What landed

- **End session button** (`web/src/ui/pane-end.ts`, `buildSessionPane`):
  Phosphor X, last in every session pane's header (after `Own tab`, also in
  a one-pane tab), the existing `killSession`, `armButton` with
  `getBehaviour().confirmEnd` read at click time (`Sure?`). Retires the A3
  "no destructive control on the header" rule — the confirm setting answers
  it.
- Two real bugs the icon button surfaced: `armButton` restored
  `textContent` (an icon button came back EMPTY after `Sure?`) → it now
  restores child nodes and drops `aria-label` while armed; `armDrag`'s ignore
  check tested `instanceof HTMLElement`, and an SVG target is not one → a
  press on a glyph inside a button started a pane drag (`Element` now).
- `.dot.is-attn` pulse stops under `prefers-reduced-motion` (found in B11).
- **Legacy gone:** the v2 handoff files (`design/session-manager/README.md`,
  `session-manager-prototype.html`, `CLAUDE_CODE_PROMPT.md`) deleted,
  references updated (`memory/log/` untouched); stale comments in
  `files.ts`, `drop-model.ts`.
- **Docs:** `web/DESIGN.md` rewritten for Nocturne (963 → ~420 lines, from
  the code), `.claude/PROJECT-SCOPE.md` (pane header bullet, Legacy
  statements), `README.md` (`What it does`), release notes final.

## Process

terminal-ui + generalist-dev (docs) in parallel, then janitor; scope review:
0 blockers, 1 should-fix (a false known-limit line in the release notes) +
4 doc/comment nits, fixed by the orchestrator (lean rule 3, no re-review);
test-engineer: suite 3449, 21 mutants on a copy, 3 survivors killed by 3
new tests (timer cleared on the ending click, reduced-motion block ORDER,
the GitHub Disconnect armButton call site). verify-terminal E1–E6 PASS
(confirm on/off, a real claude ended with no orphan, split drag still swaps,
the × does not drag, the survivor refits 70 → 149 cols, V1, both doors on an
exited pane). Suite 3436 → 3452.

## Lessons

- An icon inside a control changes what "the target" is: `instanceof
  HTMLElement` guards and `textContent` restores both silently assumed text
  buttons. Grep for them when a control gains an SVG.

User-verified on Windows ("alles werkt naar behoren"); the release waits until after C1 (user's call). Still owed: open
question for the user: the restart dialog says `History` where the Sessions
panel says `Earlier`.
