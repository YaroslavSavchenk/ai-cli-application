---
type: log
created: 2026-09-13
updated: 2026-09-13
tags: [log, nocturne, design, frontend, files-panel, sessions-panel]
---
# 2026-09-13 — Nocturne A5: Files panel + Commits list + Sessions panel LANDED

User: "alles werkt keurig, begin met stap a5" (A4b verified first). Context
[[nocturne-full-switch]]; follows [[2026-09-13-nocturne-a4b]]. Lessons in
[[files-panel-focus-owner-trap]].

Built (terminal-ui, opus; brief "a hairline ledger on the app ground"):
Files panel as a third middle-row flex sibling (Projects, Files, grid,
Sessions), `web/src/ui/files.ts` + pure `files-model.ts` + `files-mock.ts`
(v3 mock verbatim, author Sava; placeholder until B2/B3). Tabs Files /
Commits, header = focused session's project name (fallback: first alive
session, then "No session"), summary row, 26 px tree rows with Phosphor
folder + 12 per-extension badge families (`--badge-<kind>-bg/fg` tokens,
oklch, CSS-only; toml dropped → `·`), amber pulse `--t-pulse-edit: 1.2s` on
edited files and every ancestor folder, commit rows inert until A6. Grip:
200–520 px, pointer capture, ←/→ ±16, home/enter/double-click → 300,
`role=separator`, focus ring = the lit strip like `.divider`. State:
`leftPanel` (default `'files'`, auto-opens with the first alive session;
opening Files closes the Projects drawer per v3) + `filesWidth`, both in the
`ai-sm:ui:v2` localStorage bag (reader ignores unknown keys, no bump).
Sessions panel 300 px: "Running now" / "Earlier" + "Clear", "Side by side",
armed `×` → "End" (danger) / "Forget" (quiet), "Continue" / "Start again",
meta `project, tool` (the tab/pane place that truncated as "Worki…" is
gone), per-project history folders KEPT (scope pin). Shared drawer CSS
moved to the v3 look (44 px header, page ground, neutral-900 borders) —
Projects drawer inherits, coherent, not redesigned.

Review: security skipped. scope-reviewer MUST-FIX: the panel carried class
`drawer`, so `OPEN_FOCUS_OWNER_SELECTOR` matched it and, since it is open by
default, `refocusTerminal()` on window activation became a permanent no-op
(the 2026-09-08 `/login` bug reborn) → `.drawer:not(.files-panel)` for the
SCREEN half only, keys test added. Should-fix: grip painted the global
focus box; `--modalhd-h` comment lied; `aria-pressed` reported the wish, not
the screen. User-decision items the orchestrator settled (reversible, told
the user): mock data under the real project name gets ONE quiet line per
tab ("Example data until the panel reads your project / commits."); width +
wish persist; place dropped from the meta accepted. test-engineer: new
shared `tests/fake-dom.ts` harness (pointer capture, dataset, recorded
timers) driving the REAL `files.ts` and `sessions.ts` (25 + 20 tests),
+23 model tests, 18 + 14 mutants all caught after closing 3 survivors
(Esc refocus in both branches, LICENSE `·` chip on screen). Fixer 11/11;
re-review clean (residual notes applied inline: title agrees with the
persisted wish, "history" → "commits", grip hover vs focus colours, Home in
the title). Janitor: `.chip-btn.is-acc` dead, comment fixed; scope-doc
drift → Files-panel bullet + "Earlier" + v2-bag sentence written.
verify-terminal 1–9 PASS with the panel open (check 4 in layouts 1 and 4
with `top` across 200→520 drags; 9 = real Claude Code, permission prompt
not exercised), A5 probes (a)–(g) PASS. Suite 1358 → 1457; 1 fix cycle.

Owed: Windows look (user); `web/DESIGN.md` Legacy block (A8); reduced-
motion for pulses (backlog); B2/B3 delete `placeholderNote()`. Next: A6
(commit view + editor, visual) on "begin aan A6".
