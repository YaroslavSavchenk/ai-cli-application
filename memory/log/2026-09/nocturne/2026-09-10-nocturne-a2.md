---
type: log
created: 2026-09-10
updated: 2026-09-10
tags: [log, nocturne, design, frontend, copy]
---
# 2026-09-10 — Nocturne A2: shell chrome + copy rules LANDED

User: "je mag starten aan a2 en a3 daarna". Context [[nocturne-full-switch]].

Built (terminal-ui, one developer): top bar 48 px (logo tile, "Session
Manager", Files/Projects/Sessions toggles with attention pill, Connected
dot, GitHub account chip with initial avatar, inline Phosphor gear
(`ui/icons.ts`, MIT text committed), outlined-accent New session); the
Theme button and the top-bar `?` button are gone (theme popover unwired —
files stay for A8); tab strip 32 px (count pill, "Needs you", hint); statusline
26 px ("N sessions" = alive only per the v3 mock, "N panes", "N waiting for
you", "Latency N ms", "Up 2h 15m", "Keyboard shortcuts"); copy rules across
the UI (no `·`/`▸` separators, plurals, Working / Needs your answer /
Finished / Needs you, sentence-case section labels, pill `Update` /
`Restarting` / `Updating`). Latent bug fixed: the empty tab strip never
rendered `+`/hint on a session-less boot (`lastSig` sentinel).

Review: scope-reviewer 17 items (2 "blockers" were stale scope-doc pins —
the `?` top-bar button the user asked for on 2026-09-08 is replaced by the
statusline "Keyboard shortcuts" button + `?` key per the v3 handoff; the
doc now says so), test-engineer updated 22 Legacy copy pins and added
`tests/ui-copy-separators.test.ts` (deny-by-default glyph scan, state words,
plurals). Fixer round 8 items; re-review 8/8 + 4 notes fixed by the
orchestrator. Suite 1179 → 1189. verify-terminal checks 4 + 7 passed live
(real BEL, real tab switch); full pass owed in A3.

Left for later parts: empty-state green buttons + `SPLIT HERE` label (A3),
drawer meta truncation "Worki…" at 300 px (A5), `--ls-wordmark` token unused
(A8 janitor), dead theme CSS/`pane-scan` node/`UiPrefs.theme` (A8/B8).
