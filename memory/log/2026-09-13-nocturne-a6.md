---
type: log
created: 2026-09-13
updated: 2026-09-13
tags: [log, nocturne, design, frontend, commit-view, editor]
---
# 2026-09-13 — Nocturne A6: Commit view + Editor (visual, mock) LANDED

User (after A5): "file systeem ziet er goed uit, alleen … niet functioneel";
asked whether to pull B2/B3 forward → chose plan order, A6 first
([[nocturne-full-switch]]). Follows [[2026-09-13-nocturne-a5]]. Lessons in
[[hidden-grid-and-mock-coherence]].

Built (terminal-ui, opus; brief "two more instrument panels, not two
apps"): `commit-view.ts` (full-screen card replacing the pane area: Back to
sessions, title, initial avatar, `main` chip, hash chip, inert `Open on
GitHub` as an `aria-disabled` button with an `sr-only` reason, `N files
changed +A -D` + five-block bar, collapsible per-file blocks with unified
diff, `Open file` / `Changes`), `editor.ts` (column LEFT of the panes per
the v3 HTML — README says right; one-line flip documented — tabs with amber
dirty dot, Save/Saved, gutter + textarea, read-only diff tabs `Changes in
<hash>`), pure `commit-model.ts` / `editor-model.ts`, state `openCommit`,
`commitCollapsed`, `editor.tabs/active`, `edits` (none persisted), new
`ChangeKind 'screen'` the pane module ignores. Grid `hidden` while a commit
is open; `panes.render()` refuses ANY work on a hidden grid and
`refreshPaneArea()` replays on return; pane grid `flex: 0 0 46%` beside the
editor. Files panel: commit rows + tree file rows live, in-panel selected
state with `All commits`, `aria-controls` to the view's blocks. Three
honesty lines (panel / view / editor), one function each.

Review (2 fix cycles + 1 inline): security skipped. Cycle 1 MUST-FIX: the
hidden-grid guard covered rebuild + empty but not the RECONCILE path —
Ctrl+Alt+Shift+Arrow under the view built xterm on `display:none` and sent
`resize 80x24` to a live PTY → single guard at the top of `render()`.
Should-fix: pane chords live under the view; `Open file` dropped focus on
body + double rebuild → open tab first, then close, then `focusBody()`;
ligatures (`===` drawn as `≡`) → one `font-variant-ligatures: none` rule on
code surfaces (design-system rule); mock header numbers contradicted the
drawn diff → numbers COUNTED from `syntheticDiff`; missing mock content
drawn as a diff row → `null` + note row; `diffBody(hash, path)`; blank pane
area on an unresolvable hash → "This commit is not available." + Back;
`authorInitial` via `Array.from`. Cycle 2: derived numbers were flat (every
bar 3/2) → per-path FNV seed varies the cadence, tree numbers from the same
derivation; window-focus acked attention under the hidden grid; New session
under the view measured a hidden grid → running session's dims /
`lastGoodDims`; `blockDomId` collision → hash tail; pre-existing
`history.list()` tie flake → insertion-index tiebreak. Cycle 3 (orchestrator
inline): the renderer still drew the UNSEEDED diff → seeded + a
renderer-vs-header test (model-vs-model tests had passed); the final
verify then caught a live BEL under the view being acked 1 ms after it
was raised — the window-focus gate (R2) was not the path a BEL takes:
`onAttention`/`onInfo` acks in `slotEvents()` now also require
`!gridHidden()`. test-engineer:
38 + 47 mutants, 4 survivors closed; all `main.ts`/`panes.ts` wiring is
source-regex-pinned only (xterm import blocks node --test). Suite 1457 →
1569. verify-terminal 1–9 PASS with the view/editor in play (check 4 in
layouts 1 and 4, check 6 with the view covering the panes 30 s).

Owed: Windows look (user); B3 replaces `syntheticDiff`/one-diff-per-path
and the single line-number column (`oldNo/newNo`); B4 owes the unsaved-text
confirm on all four doors (tab close, reload, window close, backend grace)
and the disk write; `lastGoodDims` is global, not per-slot; editor caret/
scroll lost when a commit view opens. Next: A7 on "begin aan A7" — ask open
decision 10 (terminal colours) FIRST.
