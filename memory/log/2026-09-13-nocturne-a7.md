---
type: log
created: 2026-09-13
updated: 2026-09-13
tags: [log, nocturne, design, frontend, settings, dialogs]
---
# 2026-09-13 — Nocturne A7: Settings, Terminal colours page, Add-a-project dialog, folder picker (visual) LANDED

User: "continue met a7". Follows [[2026-09-13-nocturne-a6]]. Before any
code, open decision 10 was asked and settled — all four points the
orchestrator's advice → [[terminal-colours-shape]] (presets + custom ground
and text; ground + text only; terminal only, status colours never themed;
Settings page only). Lessons in [[parallel-halves-one-stylesheet]].

Built by TWO `terminal-ui` agents in parallel (opus), disjoint TS files and
CSS block regions, plus a third for the picker:

- **Half (a)** `settings.ts` rewritten (`initSettings` signature kept, no
  `main.ts` change): v3 modal 640 wide, 170 px left nav, five pages —
  Status bar (LIVE as before: prefs `statusLine`, notice, Reset; NEW 32 px
  preview strip led by `Example`), Preferences (MOCK until B6: provider rows
  derived from `launch-args.ts` `TOOL_CARDS`, `Uses your Claude login` /
  `Not available yet` / `No key needed`, masked `Paste API key` inputs, four
  Defaults, all `disabled`, one honesty line), Keyboard (KEY_ROWS + `all
  shortcuts`), **Terminal colours** (NEW, not in v3: 3-line mono preview,
  six preset cards = `theme-model.ts` GROUNDS×RAMPS index pairs with
  Nocturne first, Custom Ground/Text as native colour input + hex field,
  `Reset to Nocturne`; selection LOCAL — no `:root`, no terminal, no prefs
  until B9, pinned by test), Background service (existing facts, Check for
  updates, `Restart service`; confirm now says "background service" too).
  Footer `.btn-accent` outline. Top-anchored scrim like A4. Old settings
  CSS + BACKEND block + `.status-*` family replaced/deleted in place; the
  `/* ---- settings panel (status line)` header prefix kept for the A4
  block-end pin.
- **Half (b)** `newproject.ts` + `github.ts` onto `ap-`/`gh-`: header
  "Add a project", text tabs with a 2 px accent underline (`role=group` +
  `aria-pressed`, deliberately not a tablist so every tab stays in the Tab
  ring), fields sunk into `--color-bg`, 18 px checklist row for git init,
  footer Cancel `btn-quiet` + one `btn-accent`; GitHub connect card, token
  card (`Add token` neutral), device-code step, repo rows. Divergences from
  v3 kept on purpose: no "read access" copy (grant is read+write), no fake
  clone percentage, login stays mono, no hardcoded `~/projects` note.
  Gradient header, blur scrim, tile glyph, clone ink-well preview gone.
  736 lines of Legacy CSS replaced in place.
- **(c) Folder picker** `picker.ts` onto `pk-` (scope review found Browse
  opening a Legacy dialog over the Nocturne one; nobody owned it): 480 wide,
  same 250 px top anchor as the `ap-` card, mono crumb row + `↑`, quick
  zones, hairline folder rows with the Phosphor folder mark, `+ folder`,
  footer Cancel + `Choose this folder` (disabled until the first listing
  resolves — an `Enter` in that window used to hand back `''`). Deleted
  `.np-glyph`, `.form-err`, `.dirlist`, `.dir-btn`, `--dirlist-min-h`.

Review: security skipped (no endpoint/spawn/path-handling change; POST
bodies byte-identical, proven on fake-dom). Round 1 (scope + test): zero
MUST-FIX, 7 should-fix + 8 notes + F1/F2 → 13 fixes in one cycle (two
accent primaries on the GitHub tab, mock key input as `type=text`, a storage
promise while decision 4 is open, backend/service naming split, stale
"A8 deletes theme-model" headers, ArrowRight from a custom pair skipping
Nocturne, preview strip without an `Example` marker, hand-copied provider
table, `Needs a key` vs `Not available yet`, `mixHex(NaN)`, path-launched
`claude` labelled by its path — pre-existing, fixed via `isClaudeCommand`).
Round 2: zero MUST-FIX, 1 should-fix (validation errors still in the Legacy
lowercase voice) + comment notes + the picker `''` guard → cycle 2, reviewed
inline. Scope doc: settings-panel bullet and Background service naming
updated; plan A8 got a size warning (~406 alias references remain in
`app.css`; A8 is a migration, not a delete-the-block afternoon).

Tests: 1569 → 1694 (model + fake-dom drives of the real settings modal,
add-a-project dialog and picker; whole-file `sg-/ap-/gh-/pk-` parity;
18 mutants killed across both gates, no flakes over 8 runs). verify-terminal
not re-run: no PTY/WS/xterm/pane code touched. Windows visuals owed to the
user's dev window (no browser in WSL: chromium lacks `libnspr4`).

Owed / follow-ups: B6 Preferences live (decision 4 — where API keys live —
still OPEN, the lead line no longer promises "on this computer"); B9 wires
the colour page to `theme.ts` (custom pair derives `dim` via `mixHex(text,
ground, 0.55)` — decide the `--xt-*` mapping); the colour choice is not
persisted and dies on reload (by design until B9); mock never shows a
"Key added" state; no `×` on the settings modal (v3 has none); picker list
has no arrow-key row navigation. Next: A8 on "begin aan A8".
